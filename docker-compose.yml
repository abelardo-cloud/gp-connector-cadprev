services:
  gp-connector-cadprev:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gp-connector-cadprev
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
    restart: unless-stopped
